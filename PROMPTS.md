Act as an expert UI/UX designer and frontend developer. Look at the existing DevConf 2026 landing page layout: it features dark minimalist headers, blue accent action buttons, clean typography, distinct speaker badges (like AI/ML, Cloud), and a structured pricing card grid.

I need to generate a brand new section called 'Explore the Learning Tracks' to replace the 'Something Missing?' place holder Because I have only learned HTML and CSS so far, the section must not use any JavaScript.

Requirements:

Use pure HTML and clean, modern CSS.

Implement CSS Grid layout ( grid-template-columns) to display 4 different technical tracks that perfectly align with the existing content layout.

Use advanced but approachable CSS features like pseudo-elements ( ::before), custom transitions ( cubic-bezier), and subtle transform modifications ( translateY) to make the track cards feel uniquely interactive on hover without breaking the page layout.

Keep font choices, weight scales, and badging colors matched cleanly to the aesthetic of the provided image screenshot.